{
  "name": "bigger-blacker-book-support",
  "private": true,
  "scripts": {
    "cleanup:test-only-tickets": "ts-node --transpile-only scripts/cleanup-test-only-tickets.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
